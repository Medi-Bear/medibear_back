<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.medibear.mapper.UserMapper">

    <select id="findById" parameterType="string" resultType="com.app.medibear.model.User">
        SELECT * FROM users WHERE user_id = #{id}
    </select>

</mapper>